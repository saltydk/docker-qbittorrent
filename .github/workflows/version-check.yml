name: version-check

on:
  workflow_dispatch:
  schedule:
    - cron:  '0 */6 * * *'

jobs:
  find-latest-release:
    runs-on: ubuntu-latest
    outputs:
      qbt-libtorrent2-version: ${{ steps.versions.outputs.qbt-libtorrent2-version }}
      qbt-libtorrent1-version: ${{ steps.versions.outputs.qbt-libtorrent1-version }}
      qbt-legacy-version: ${{ steps.versions.outputs.qbt-legacy-version }}
      qbt-legacy-revision: ${{ steps.versions.outputs.qbt-legacy-revision }}
      qbt-latest-revision: ${{ steps.versions.outputs.qbt-latest-revision }}

    steps:
      - name: Get and validate versions
        id: versions
        run: |
          # Function to safely get and validate JSON from URL
          get_json() {
            local url="$1"
            local response
            local retries=3
            
            for ((i=1; i<=retries; i++)); do
              response=$(curl -sL --retry 3 --retry-delay 5 "$url")
              if [ $? -eq 0 ] && echo "$response" | jq empty > /dev/null 2>&1; then
                echo "$response"
                return 0
              fi
              sleep 5
            done
            return 1
          }

          # Get main versions
          main_json=$(get_json "https://github.com/userdocs/qbittorrent-nox-static/releases/latest/download/dependency-version.json")
          if [ $? -ne 0 ]; then
            echo "::error::Failed to fetch main versions"
            exit 1
          fi

          # Get legacy versions
          legacy_json=$(get_json "https://github.com/userdocs/qbittorrent-nox-static-legacy/releases/latest/download/dependency-version.json")
          if [ $? -ne 0 ]; then
            echo "::error::Failed to fetch legacy versions"
            exit 1
          fi

          # Extract and set outputs using GitHub Actions syntax
          echo "qbt-libtorrent2-version=release-$(echo "$main_json" | jq -r .qbittorrent)_v$(echo "$main_json" | jq -r .libtorrent_2_0)" >> $GITHUB_OUTPUT
          echo "qbt-libtorrent1-version=release-$(echo "$main_json" | jq -r .qbittorrent)_v$(echo "$main_json" | jq -r .libtorrent_1_2)" >> $GITHUB_OUTPUT
          echo "qbt-legacy-version=release-$(echo "$legacy_json" | jq -r .qbittorrent)_v$(echo "$legacy_json" | jq -r .libtorrent_1_2)" >> $GITHUB_OUTPUT
          echo "qbt-legacy-revision=$(echo "$legacy_json" | jq -r .revision)" >> $GITHUB_OUTPUT
          echo "qbt-latest-revision=$(echo "$main_json" | jq -r .revision)" >> $GITHUB_OUTPUT

  update-libtorrent2:
    runs-on: ubuntu-latest
    needs: find-latest-release
    steps:
    - uses: actions/checkout@v4
      with:
        persist-credentials: false # otherwise, the token used is the GITHUB_TOKEN, instead of your personal access token.
        fetch-depth: 0 # otherwise, there would be errors pushing refs to the destination repository.
        ref: libtorrent2
    - name: Create local changes
      run: |
        [ ! -z "$QBT_VERSION" ] && [[ "$QBT_VERSION" != "null" ]] && sed -i 's/.*ARG QBITTORRENT_RELEASE=.*/ARG QBITTORRENT_RELEASE="'${QBT_VERSION}'"/' Dockerfile
      env:
        QBT_VERSION: ${{ needs.find-latest-release.outputs.qbt-libtorrent2-version }}
    - name: Create local changes
      run: |
        [ ! -z "$QBT_REVISION" ] && [[ "$QBT_REVISION" != "null" ]] && sed -i 's/.*ARG QBITTORRENT_REVISION=.*/ARG QBITTORRENT_REVISION="'${QBT_REVISION}'"/' Dockerfile
      env:
        QBT_REVISION: ${{ needs.find-latest-release.outputs.qbt-latest-revision }}
    - name: Commit files
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -m "Update to ${{ needs.find-latest-release.outputs.qbt-libtorrent2-version }}-${{ needs.find-latest-release.outputs.qbt-latest-revision }}" -a || echo "Nothing to update"
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GH_TOKEN }}
        branch: libtorrent2

  update-libtorrent1:
    runs-on: ubuntu-latest
    needs: find-latest-release
    steps:
    - uses: actions/checkout@v4
      with:
        persist-credentials: false # otherwise, the token used is the GITHUB_TOKEN, instead of your personal access token.
        fetch-depth: 0 # otherwise, there would be errors pushing refs to the destination repository.
        ref: libtorrent1
    - name: Create local changes
      run: |
        [ ! -z "$QBT_LIBTORRENT1_VERSION" ] && [[ "$QBT_LIBTORRENT1_VERSION" != "null" ]] && sed -i 's/.*ARG QBITTORRENT_RELEASE=.*/ARG QBITTORRENT_RELEASE="'${QBT_LIBTORRENT1_VERSION}'"/' Dockerfile
      env:
        QBT_LIBTORRENT1_VERSION: ${{ needs.find-latest-release.outputs.qbt-libtorrent1-version }}
    - name: Create local changes
      run: |
        [ ! -z "$QBT_REVISION" ] && [[ "$QBT_REVISION" != "null" ]] && sed -i 's/.*ARG QBITTORRENT_REVISION=.*/ARG QBITTORRENT_REVISION="'${QBT_REVISION}'"/' Dockerfile
      env:
        QBT_REVISION: ${{ needs.find-latest-release.outputs.qbt-latest-revision }}
    - name: Commit files
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -m "Update to ${{ needs.find-latest-release.outputs.qbt-libtorrent1-version }}-${{ needs.find-latest-release.outputs.qbt-latest-revision }}" -a || echo "Nothing to update"
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GH_TOKEN }}
        branch: libtorrent1

  update-legacy:
    runs-on: ubuntu-latest
    needs: find-latest-release
    steps:
    - uses: actions/checkout@v4
      with:
        persist-credentials: false # otherwise, the token used is the GITHUB_TOKEN, instead of your personal access token.
        fetch-depth: 0 # otherwise, there would be errors pushing refs to the destination repository.
        ref: legacy
    - name: Create local changes
      run: |
        [ ! -z "$QBT_LEGACY_VERSION" ] && [[ "$QBT_LEGACY_VERSION" != "null" ]] && sed -i 's/.*ARG QBITTORRENT_RELEASE=.*/ARG QBITTORRENT_RELEASE="'${QBT_LEGACY_VERSION}'"/' Dockerfile
      env:
        QBT_LEGACY_VERSION: ${{ needs.find-latest-release.outputs.qbt-legacy-version }}
    - name: Create local changes
      run: |
        [ ! -z "$QBT_REVISION" ] && [[ "$QBT_REVISION" != "null" ]] && sed -i 's/.*ARG QBITTORRENT_REVISION=.*/ARG QBITTORRENT_REVISION="'${QBT_REVISION}'"/' Dockerfile
      env:
        QBT_REVISION: ${{ needs.find-latest-release.outputs.qbt-legacy-revision }}
    - name: Commit files
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -m "Update to ${{ needs.find-latest-release.outputs.qbt-legacy-version }}-${{ needs.find-latest-release.outputs.qbt-legacy-revision }}" -a || echo "Nothing to update"
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GH_TOKEN }}
        branch: legacy
